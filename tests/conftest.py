"""Shared pytest fixtures for the unit-test suite.

Intentionally empty — unit tests should depend only on stdlib + the modules
under test. Heavy fixtures (API, DB) belong in ``eval/conftest.py``.
"""
